import { LitElement, html } from 'lit';

export class MarkdownEditor extends LitElement {
  render() {
    return html`<p>DOOOOKIE</p>`;
  }
}

customElements.define('markdown-editor', MarkdownEditor);